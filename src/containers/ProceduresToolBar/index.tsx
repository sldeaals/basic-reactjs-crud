import React, { memo } from 'react';
import { TableToolBar } from '../../components';

const ProceduresToolBar: React.FC = memo(() => {
  return <TableToolBar title="Procedures" />;
});

export default ProceduresToolBar;
